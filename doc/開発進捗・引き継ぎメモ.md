# MIR（音楽解析・自動タグ付けシステム）開発進捗・引き継ぎメモ

**最終更新**: 2026-01-09

---

## 📋 プロジェクト概要

MIR（Music Information Retrieval）技術を活用し、ローカルの音楽ファイルを解析して「ジャンル」「楽器」「開始拍」などのメタデータを自動生成。最終的にrekordboxへ反映させるパイプラインを構築する。

### ゴール
- 音楽ファイルを解析してタグを自動生成
- rekordboxのComment欄にタグを書き込み
- 後から拡張可能なプラグイン設計

---

## ✅ 完了したこと

### 1. 環境セットアップ
- **Python 3.10 ARM64ネイティブ環境**: M1 Pro Mac対応
- **Essentia**: pipインストール成功（arm64ネイティブ）
- **Essentia TensorFlow**: MusiCNNモデル使用可能
- **PyTorch + MPS**: Apple Silicon GPU加速対応
- **PANNs**: AudioSet事前学習モデル（527クラス）
- **Demucs**: 音源分離モデル（htdemucs）
- **Mutagen**: ID3タグ書き込み
- **librosa**: 拍解析

### 2. プロジェクト構造
```
mir-tagger/
├── doc/
│   ├── 音楽解析・自動タグ付けシステム開発手順書.md
│   ├── 開発進捗・引き継ぎメモ.md（このファイル）
│   ├── 機能リファレンス.md           # 🆕 詳細リファレンス
│   └── 環境構築実施記録_2026-01-08.md
├── src/
│   ├── core/
│   │   ├── audio_loader.py      # 音声読み込み（Essentia使用）
│   │   └── base_analyzer.py     # 基底クラス + AnalyzerRegistry
│   ├── analyzers/               # プラグイン形式アナライザー
│   │   ├── key_analyzer.py      # Key検出 ✅
│   │   ├── danceability_analyzer.py  # Danceability ✅
│   │   ├── energy_analyzer.py   # Energy ✅
│   │   ├── genre_analyzer.py    # 🆕 ジャンル分類 ✅
│   │   ├── beat_analyzer.py     # 🆕 拍解析 ✅
│   │   └── intro_analyzer.py    # 🆕 イントロ解析 ✅
│   ├── metadata/
│   │   └── tag_writer.py        # タグ書き込み（Mutagen使用）✅
│   ├── export/                  # (未実装) rekordbox XML
│   ├── config.py
│   └── main.py                  # CLIエントリポイント
├── tests/
│   └── samples/
├── venv/                        # ARM64ネイティブ環境
├── requirements.txt
├── batch_analyze.py             # 一括処理スクリプト ✅
└── mir-tagger.code-workspace    # VSCodeワークスペース
```

### 3. 実装済み機能

| 機能 | ファイル | 状態 | 使用モデル/ライブラリ |
|------|---------|------|----------------------|
| 音声読み込み | `src/core/audio_loader.py` | ✅ | Essentia |
| Key検出 | `src/analyzers/key_analyzer.py` | ✅ | Essentia Key |
| Danceability | `src/analyzers/danceability_analyzer.py` | ✅ | Essentia |
| Energy | `src/analyzers/energy_analyzer.py` | ✅ | Essentia |
| ジャンル分類 | `src/analyzers/genre_analyzer.py` | ✅ | MusiCNN + PANNs + Essentiaルール |
| 拍解析 | `src/analyzers/beat_analyzer.py` | ✅ | librosa + PANNs |
| イントロ解析 | `src/analyzers/intro_analyzer.py` | ✅ | Demucs + PANNs |
| タグ書き込み | `src/metadata/tag_writer.py` | ✅ | Mutagen |
| CLI | `src/main.py` | ✅ | - |
| 一括処理 | `batch_analyze.py` | ✅ | - |

### 4. 動作確認結果

**サンプル曲「星間飛行」（アニソン）の解析結果:**

| タグ | 結果 | 備考 |
|------|------|------|
| Key | Eb（変ホ長調） | 強度90% |
| Danceability | High | スコア1.14 |
| Energy | VeryHigh | - |
| Genre | pop, anime | 複数ジャンル検出 |
| Downbeat | 1拍目 | セリフスキップ後 |
| SpeechIntro | 5秒 | 「みんな抱きしめて〜」 |
| MusicAt | 5秒 | 音楽開始位置 |

**最終タグ出力:**
```
[Downbeat:1][Dance:High][Energy:VeryHigh][Genre:pop][Genre:anime][SpeechIntro:5s][MusicAt:5s][Key:Eb]
```

**rekordbox連携**: Comment欄に書き込み → 「タグを再読み込み」で反映確認済み ✅

---

## 🔧 使い方

### 単一ファイル解析
```bash
# 解析のみ（JSON出力）
python -m src.main "path/to/music.mp3"

# タグ形式出力
python -m src.main "path/to/music.mp3" -f tags

# 解析 + タグ書き込み（バックアップ付き）
python -m src.main "path/to/music.mp3" -w --backup
```

### 一括処理
```bash
# ドライラン（処理対象確認）
python batch_analyze.py /path/to/music --dry-run

# 解析 + タグ書き込み
python batch_analyze.py /path/to/music -w --backup

# 並列処理（4プロセス）
python batch_analyze.py /path/to/music -w -j 4
```

### 利用可能なアナライザー一覧
```bash
python -m src.main -l
```

---

## 🔌 新しいアナライザーの追加方法

`src/analyzers/` に新しいファイルを作成するだけで自動登録される：

```python
# src/analyzers/my_analyzer.py
from src.core.base_analyzer import BaseAnalyzer, AnalyzerRegistry

@AnalyzerRegistry.register
class MyAnalyzer(BaseAnalyzer):
    name = "my_tag"
    description = "My custom analyzer"
    tag_prefix = "MyTag"
    
    def analyze(self, audio, sample_rate):
        # 解析ロジック
        return {'value': 'something'}
    
    def to_tag(self, result):
        return f"[{self.tag_prefix}:{result['value']}]"
```

---

## 📱 rekordbox連携について

### 確認済み事項
- **ID3タグ埋め込み方式を採用**（rekordbox.xml編集ではなく）
- Comment欄（desc=""）にタグを書き込む
- rekordboxで「**タグを再読み込み**」すると反映される
- **CUE/ビートグリッドは保持される**（再読み込みでは上書きされない）
- BPMはrekordboxの解析値をそのまま使う（上書きしない）

### マイタグについて
- rekordboxの「マイタグ」機能は外部から直接設定不可
- 代わりにComment欄を活用（検索可能）
- **スマートプレイリスト**でComment欄の条件検索が可能

---

## 🚀 今後のプラン（実装順序）

### ~~② ジャンル分類~~ ✅ **完了**
- ~~Essentiaの特徴量 + 分類モデル~~
- MusiCNN + PANNs + Essentiaルールのアンサンブルで実装

### ~~③ 拍解析~~ ✅ **完了**
- ~~Downbeat検出（何拍目から始まるか）~~
- ~~BPM変化検出（途中でテンポが変わる曲）~~
- librosaで実装（madmomは依存関係問題のため不採用）
- セリフ自動スキップ機能付き

### ~~④ 音源分離 + 高度な検出~~ ✅ **完了**
- ~~ボーカル/ドラム/ベース/その他の分離~~
- ~~「歌で始まる」検出~~
- ~~「セリフで始まる」検出~~
- Demucs + PANNsで実装

### ⑤ 追加機能（予定）
- [ ] 「途中でキメが入る」検出
- [ ] 「長いビルドアップ」検出
- [ ] 楽曲構造解析（Verse/Chorus/Bridge）
- [ ] rekordbox XML出力

---

## ⚠️ 注意事項

### M1 Pro Mac（Apple Silicon）
- **ARM64ネイティブ環境を使用**
  - `/opt/homebrew/bin/python3.10`（ARM64）
  - `venv/` は ARM64ネイティブ
- Essentia: ✅ 正常動作
- Essentia TensorFlow: ✅ 正常動作（MusiCNN使用可）
- PyTorch: ✅ MPS加速対応
- Demucs: ✅ 正常動作
- librosa: ✅ 正常動作

### バックアップ
- 一括処理時は `--backup` オプション推奨
- バックアップファイルは元ファイルと同じディレクトリに作成

### パフォーマンス
- 1曲あたり約10〜30秒（Demucs使用時）
- MPS加速により高速化

---

## 📦 依存パッケージ

```
# requirements.txt（2026-01-09時点）
essentia>=2.1b6              # 基本音声解析
essentia-tensorflow>=2.1b6   # MusiCNNモデル
mutagen>=1.47.0              # ID3タグ書き込み
librosa>=0.10.0              # 拍解析
torch>=2.0.0                 # PANNs/Demucs用
torchaudio>=2.0.0            # 音声処理
demucs>=4.0.0                # 音源分離
panns-inference>=0.1.0       # PANNsモデル
numpy>=1.25
pyyaml>=6.0
tqdm>=4.66.0
```

---

## 💡 ユーザーの希望・方針

1. **BPMの反映は考えていない** - rekordboxの解析値をそのまま使用
2. **後から拡張できる設計** - プラグイン形式で実現済み
3. **高精度優先** - 音源分離はDemucsを使用
4. **基本から段階的に実装** - 完了
5. **セリフで始まる曲への対応** - PANNsで検出、拍解析から除外

---

## 📄 関連ドキュメント

- **[機能リファレンス.md](./機能リファレンス.md)** - 各機能の詳細仕様、パラメータ、カスタマイズ方法
- **[環境構築実施記録_2026-01-08.md](./環境構築実施記録_2026-01-08.md)** - 環境セットアップの詳細ログ

---

## 📁 テスト用サンプル

- `/Users/hanyanty/Music/Music/Media.localized/Music/ランカ・リー＝中島 愛/「マクロスF(フロンティア)」VOCAL COLLECTION 娘たま♀ (Disc1)/10 星間飛行.mp3`
